{
  "name": "crimp-studio",
  "version": "0.0.1",
  "private": true,
  "description": "Climbing wall designer and route generator",
  "scripts": {
    "dev": "pnpm --filter @crimp-studio/web dev",
    "build": "pnpm --filter @crimp-studio/web build",
    "preview": "pnpm --filter @crimp-studio/web preview",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "ci:flow": "pnpm typecheck && pnpm lint && pnpm test"
  },
  "keywords": [
    "climbing",
    "bouldering",
    "route-generation",
    "three.js",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
